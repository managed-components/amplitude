{
  "name": "Amplitude",
  "description": "Amplitude Managed Component",
  "namespace": "amplitude",
  "icon": "assets/amplitude.svg",
  "categories": ["Analytics"],
  "provides": ["events"],
  "allowCustomFields": true,
  "permissions": {
    "access_client_kv": {
      "description": "Amplitude uses cookies to attribute sessions more accurately",
      "required": true
    },
    "server_network_requests": {
      "description": "Amplitude uses serverside network requests to report session data",
      "required": true
    }
  }
}
